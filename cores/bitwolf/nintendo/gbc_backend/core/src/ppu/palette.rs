#[derive(Clone, Copy, Debug)]
pub(super) enum Palette {
    BGP,
    OBP0,
    OBP1,
}

#[derive(Clone, Copy, Debug)]
pub(super) enum Index {
    I0,
    I1,
    I2,
    I3,
}

#[derive(Clone, Copy, Debug)]
pub(super) enum Colour {
    C0,
    C1,
    C2,
    C3,
}
